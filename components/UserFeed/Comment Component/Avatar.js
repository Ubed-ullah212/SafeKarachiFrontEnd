import React from "react";
import { IconButton, Avatar, Searchbar } from "react-native-paper";
const CustomAvatar = ({ source }) => {
  return <Avatar.Image size={30} source={source} />;
};
export default CustomAvatar;
